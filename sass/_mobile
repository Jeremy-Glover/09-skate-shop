@media screen and (max-width: 640px) {
//Header
  .fa {
    font-size: 1.2rem;
  }

  .fa-circle {
    display: none;
  }

  .subscribe {
    margin: 0;
  }

  .nav-site {
    display: flex;
    flex-wrap: wrap;
    margin: 2rem;
  }

  .nav-item {
    display: flex;
    margin: .5rem;
    justify-content: center;
    border: none;
    font-size: .6rem;
  }

  //Skateboard Professionals
  .skater-links__items {
    justify-content: center;
  }

  .arrow, .arrow:after {
    display: none;
  }
}
